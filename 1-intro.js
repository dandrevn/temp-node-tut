//Modules - Encapsulated Code (sharing only what we want)
//Every file is a module (by default);
const amount = 12; 

if (amount < 10) {
    console.log('small number');
}else {
    console.log('large number');
}

console.log('Hey its my first Node App!');